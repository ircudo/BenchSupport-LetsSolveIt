package pointers;

// Valid Palindrome II

public class TaskE3_Solution {
    public static void main(String[] args) {
        String toCheck = "A man, a plan, a canal, Panama!";
        System.out.println(validPalindrome(toCheck));
    }
    private static boolean validPalindrome(String toCheck) {

        // Your solution goes here

        // Since we'll be checking on valid palindrome on each shift of the pointers,
        // we might find it helpful to crate a method validPalindromeUtil, to take in String s and  two indices and return a boolean

        return true;
    }


}
