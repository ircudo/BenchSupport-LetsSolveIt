package arrays_hashing;

import java.util.*;

// Largest Number
public class TaskM3_Solution {
    public static void main(String[] args) {
        int[] array = {3, 30, 34, 5, 9};
        System.out.println(largestNumber(array));
    }

    public static String largestNumber(int[] nums) {
        // create array of strings

        // populate the array with nums


        // Define a custom comparator to sort the strings


        // Sort the string array using the custom comparator


        // Handle edge case where the largest number is 0


        // Concatenate the sorted strings to form the largest number

        return null;
    }
}
